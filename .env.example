# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
# Optional model override
# CLAUDE_MODEL=claude-haiku-4-5

# Postgres connection. For local dev with docker-compose:
DATABASE_URL=postgres://chaji:chaji@localhost:5432/chaji?sslmode=disable
# In production (DO managed Postgres) this is auto-injected as ${db.DATABASE_URL}.

# Random string used to sign session cookies. Generate with: openssl rand -hex 32
SESSION_SECRET=replace-me-with-a-long-random-string
